from src.triangle import Triangle
from src.matrix import Matrix

# Sample Usage:
# triangle = Triangle(a=4, b=6, B=80)
# triangle.solve()
# triangle.graph()

matrix = Matrix([[3, 1], [1, 2]])
matrix2 = Matrix([[-1, 2, 2], [4, -1, 5], [3, -4, 5]])
# matrix.graph2D(vector=(-1, 2), scale=5)
# matrix.graph2D(scale=5)
# matrix2.graph3D(scale=10)
# matrix.graphBasis(scale=5)
#matrix.graphBasis(column=0, scale=6)